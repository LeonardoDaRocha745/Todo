export interface Todo{
    //o nome das variaveis tem q ser igual a os q foi declarado no backend 
    id?: String,
    tituloString: String, 
    descricao: String ,
    dataParaFinalizar: any,
    finalizado: Boolean 
}